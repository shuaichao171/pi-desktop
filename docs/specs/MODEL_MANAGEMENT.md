# 模型管理

2026-09-26：参考 ZCode 的 `ModelProviderSection`、`SectionLayout`、`InlineEditableProviderCard`、`ProviderFormControls` 与模型元数据对话框，调整 Pi 模型管理。

- 页面保持供应商导航和详情两栏。刷新、添加供应商位于页头，当前会话模型和思考级别为次要信息。
- 自定义可编辑供应商在详情直接编辑名称、地址、协议；请求头与系统代理收在高级配置。Pi 写入完整供应商配置，使用明确保存/取消事务，不直接照搬 ZCode 的逐字段自动保存。
- 内置及不兼容的自定义配置只读连接字段；凭据使用已有独立接口。API Key 留空保持原值，失败保留输入；请求头密文保留与代理继承语义不变。
- 模型列表持续显示，添加/编辑在独立模态对话框中完成；成功才关闭，失败保留草稿，保存中禁止关闭与重复提交。重复模型 ID、容量、Google 独立输出上限及思考映射继续校验。
- 获取模型为独立导入事务：使用已保存连接读取目录，搜索/勾选新增模型，保留原有模型；确认才写入。关闭或过期响应不得修改目录。它不是推理连通性测试。
- 新供应商先选模板，再编辑连接和选择/添加至少一个模型，一次创建；取消不落盘。模板不会覆盖已有供应商。
- 连接、凭据、模型、导入及新建草稿统一上报 dirty/saving。切供应商、切设置分类、刷新与关闭有未保存保护；继续编辑返回原焦点，放弃才执行目标动作。所有写入串行，回包绑定当前工作区和操作身份。
- 深浅主题、中文/英文及大字号使用 Pi 语义变量；窄窗口导航保持可用；对话框使用模态焦点、Esc 和焦点返回。

验收：真实构建 renderer 的模拟 bridge 验证编辑/失败/取消/保存、导入去重、导航保护、凭据保留和忙状态；检查深浅主题及 1440/900/680px 截图。运行模型/供应商相关回归、类型检查与构建。不使用真实凭据或调用在线模型。

本次验证结果：

- 四个工作区包类型检查及生产构建通过；全套测试 459 项通过、2 项按平台跳过、0 失败。
- 默认字号下深浅主题及 1440/900/680px 截图已检查；模型、新建与导入对话框居中检查通过。680px 与 16px 字号组合未运行。
- 最新构建的 42 条交互回归通过，覆盖模型失败取消后再次编辑、模型与导入保存、关闭后的迟到响应、连接/凭据草稿隔离、两类离开保护与焦点/选区恢复、忙状态与当前模型保护。测试使用虚构 bridge，不代表真实在线模型连通性验证。
- 保留 `tests/fixtures/model-settings` 及 `pnpm test:model-settings-ui` 作为可重复执行的真实构建回归入口；诊断确认连接与凭据必须使用独立 React key，重置草稿后详情只能存在一个连接表单。
