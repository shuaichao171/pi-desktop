# 项目审查与修复记录（2026-09-24）

本次检查了 Pi SDK 会话适配、Electron 主进程和 utility process 通信、React 状态与输入区、工作台 Git/命令服务、更新流程及现有测试。基线测试为 48 通过、1 个 Unix 专属测试跳过。`issues.txt` 中多数旧条目已有对应修复，本次保留该文件，按当前实现复现问题后修改。

## 已修复的问题

| 问题与触发条件 | 修复结果 | 主要源码 |
| --- | --- | --- |
| 并发拖放/粘贴附件时，后完成的读取覆盖其他附件；发送等待期间编辑并切换会话，旧发送结果可能清空新草稿 | 每次读取完成后合并最新附件并重新检查数量；发送只清理未修改的对应草稿；同步阻止重复发送 | `packages/ui/src/components/Composer.tsx`、`packages/ui/src/composerDrafts.ts` |
| 切换工作区或替换 bridge 后，旧模型/凭据请求覆盖新状态；过期失败覆盖成功结果 | 按上下文和请求序号隔离结果与 loading 计数；替换 bridge 立即清理旧可发送状态 | `packages/ui/src/store.ts` |
| 开关扩展时，枚举阶段尚未占用配置锁，允许发送或第二次开关并发进入 | 在首个异步操作前占用配置锁，覆盖整个设置与重载过程 | `packages/agent/src/index.ts` |
| Pi 扩展命令抛错被 SDK 转交回调，桌面未接收该回调 | 将扩展错误发送到桌面错误事件 | `packages/agent/src/index.ts` |
| 修改全局 API Key 后，其他已加载会话仍保留旧的模型可用性与登录状态 | 同步刷新全部已加载运行时；发生错误也等待所有刷新完成后才释放操作并允许销毁 | `packages/agent/src/index.ts` |
| 关闭 Agent 时仍接受新 RPC，正常退出可能触发崩溃恢复 | 立即关闭请求入口、拒绝仍在等待启动的普通调用；关闭操作幂等，主动退出不触发恢复 | `packages/desktop/src/main/agentClient.ts` |
| 自动恢复尚未完成时切换工作区，两个会话切换相互冲突 | 自动恢复和工作区切换共用串行队列，最终保留用户选择 | `packages/desktop/src/main/ipc.ts` |
| 工作区是仓库子目录时 Git 路径错误；未跟踪目录折叠后无法预览；首次提交前暂存差异失败 | 转换成工作区相对路径；逐文件列出未跟踪内容；无 HEAD 时使用 Git 计算的空树进行比较 | `packages/desktop/src/main/workbenchService.ts` |
| 命令输出超过限制后重复报错，进程退出事件可能早于最后输出，停止中的命令被过早移出跟踪 | 超限仅报错一次；输出关闭后报告完成；重置等待正在执行的终止操作 | `packages/desktop/src/main/workbenchService.ts` |
| 更新自动下载的独立 Promise 拒绝未处理；安装器只发出 error 事件时安装状态永久锁定 | 捕获下载失败；统一处理同步和异步安装失败，恢复检查并保留重启提示 | `packages/desktop/src/main/updateService.ts` |

## 验证结果

- `pnpm test`：65 项，64 通过，1 项 Unix 进程组测试在 Windows 跳过，无失败。
- `pnpm typecheck`：全部工作区通过。
- `pnpm build`：Electron 主进程、preload 和 React renderer 构建通过。
- `node scripts/sync-pi.mjs --verify`：Pi SDK、锁文件、排除项和 Pi Chord esbuild 配置一致。
- `git diff --check`：通过。
- 使用独立 profile 和模拟 bridge 运行隐藏的 Electron 渲染验证：并发附件、重复发送保护、发送期间编辑、跨会话草稿恢复均通过；已查看深色桌面和浅色窄窗口截图。验证进程已退出，未使用真实凭据或发送模型请求。

界面验证脚本与图片在本机忽略目录 `out/review/`，不进入提交。

## 验证范围与剩余限制

- 本次在 Windows 验证，未实际运行 macOS/Linux 应用，也未执行签名、安装包发布或真实在线更新。
- 安装更新前 Agent 服务会关闭。如果安装器随后失败，应用明确提示重启；当前不会在同一进程内重新创建已关闭的服务。
- 界面验证使用模拟 bridge；真实 Pi SDK 初始化、信任、扩展和模型配置由集成测试覆盖，未调用付费模型进行端到端生成。

## 后续复查（当前工作区）

基于已有未提交改动继续审查，保留上面的历史记录。本轮起始基线为 185 项测试（184 通过、1 项跳过），覆盖 Agent 会话管理、主进程通信与退出、UI 异步状态、搜索/上下文、工作台文件/Git 服务及构建配置。本轮修复了以下 12 类可确认问题，新增 15 项回归测试。

| 问题与触发条件 | 修复结果 | 源码与回归测试 |
| --- | --- | --- |
| 扩展已经预留目标会话，桌面初始化仍可打开同一历史文件，产生两个写入运行时 | 初始化显式会话时检查扩展预留 | `agent/src/index.ts`；`slash-commands.test.mjs` |
| 会话切换开始后的第一个异步等待期间，发送或设置操作仍落入旧会话 | 切换开始后立即拒绝依赖当前会话的操作 | `agent/src/index.ts`；`slash-commands.test.mjs` |
| RPC 等待旧 Agent ready 后，旧进程立即退出，调用被转发到替代进程 | 每个调用绑定原 Agent 进程；代际变化时拒绝，避免旧提示词重放 | `desktop/src/main/agentClient.ts`；`agent-client.test.mjs` |
| 服务关闭失败提前中断其他清理和元数据刷写，扩展交互仍等待答复 | 关闭幂等，取消交互，等待全部服务和最终元数据队列，再报告错误 | `desktop/src/main/ipc.ts`；`agent-ipc.test.mjs` |
| 安装更新前关闭失败被吞掉，安装器仍启动 | 传播关闭错误，阻止安装并显示既有重启提示 | `desktop/src/main/index.ts`；`startup.test.mjs`、`update-service.test.mjs` |
| 重试初始化等待工作区或失败期间发生新导航，旧请求污染新上下文 | 重试使用导航请求序号，忽略过时调用与错误 | `ui/src/store.ts`；`store.test.mjs` |
| 旧停止请求失败时覆盖新会话错误状态 | 按 bridge、导航和会话身份限定失败的作用范围 | `ui/src/store.ts`；`store.test.mjs` |
| 工作区列表请求乱序完成，旧结果覆盖新列表 | 仅接收最新列表请求的结果与错误 | `ui/src/store.ts`；`store.test.mjs` |
| 从 A 切向 B 尚未完成时再次点击 A，被当作无需操作，最终仍进入 B | 存在待处理导航时允许重新选择当前工作区 | `ui/src/store.ts`；`store.test.mjs` |
| 搜索文件结果先到，选中后会话结果插入上方，Enter 打开另一对象 | 按查询和结果 ID 保存选择 | `ui/src/components/SearchDialog.tsx`；`search-selection.test.mjs` |
| 未跟踪文件差异等待 Git 时切换工作区，读取到另一项目的同名文件 | 差异读取全程使用最初工作区根路径并保留路径边界校验 | `desktop/src/main/workbenchService.ts`；`workbench.test.mjs` |
| 未跟踪文件的非法 UTF-8 尾部被静默删除，截断中文差异可能产生乱码 | 严格拒绝非法编码；只有截断预览可以省略末尾不完整字符 | `desktop/src/main/workbenchService.ts`；`workbench.test.mjs` |

上表源码路径以 `packages/` 为根，测试路径以 `tests/` 为根。

### 本轮验证

- `pnpm test`：200 项，199 通过、1 项 Unix 进程组测试在 Windows 跳过，无失败。
- `pnpm typecheck`、`pnpm build`、`node scripts/sync-pi.mjs --verify`、`git diff --check`：均通过。
- 独立隐藏 Electron 测试窗口实际验证了“选中第二个文件 → 延迟会话结果插入 → Enter 打开原选中文件”；检查了深色 1280×820 和浅色 780×820 截图，选择高亮与布局正常。
- UI 验证脚本与截图位于本机忽略目录 `out/review/round2/`，验证窗口和进程已退出。
- Agent 进程绑定、关闭和元数据顺序另经独立只读复核，未发现本轮修改引入的确证问题。

本轮未进行真实模型请求、安装包发布或在线升级；macOS/Linux 实机运行仍未验证。没有提交 Git，原有未提交改动均保留。
